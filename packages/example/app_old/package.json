{
  "name": "@example/app-old",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@example/engine-browser": "1.0.0",
    "@example/engine-server": "1.0.0",
    "@example/tenant-dark": "1.0.0",
    "@example/tenant-light": "1.0.0"
  },
  "devDependencies": {
    "@dev/eslint": "1.0.0",
    "@example/webpack": "1.0.0"
  },
  "scripts": {
    "build": "yarn webpack build --mode development",
    "prod": "yarn webpack build",
    "dev": "yarn webpack serve --mode development",
    "ssr": "yarn dev --env render=csr",
    "csr": "yarn dev --env render=csr",
    "instrument": "yarn prod --env instrument",
    "lint": "yarn eslint .",
    "debug-webpack": "yarn prod --debug",
    "debug-server": "yarn dev --env debug"
  }
}
